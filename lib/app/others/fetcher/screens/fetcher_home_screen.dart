import 'package:flutter/material.dart';

class FetcherHomeScreen extends StatelessWidget {
  const FetcherHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fetcher Home')),
      body: Placeholder(),
    );
  }
}
