import 'package:flutter/material.dart';

class TextReaderBookmarkButton extends StatelessWidget {
  const TextReaderBookmarkButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
